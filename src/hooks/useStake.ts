// import { useCallback } from 'react';
// import { getERC20Contract } from '../utils/index';
// import { getContract } from '../utils/index';
// import web3 from 'web3';
// import stakeZWZAbi from '../config/abi/stakeZWZAbi.json';
// import BigNumber from 'bignumber.js';

// import useActiveWeb3React from './useActiveWeb3React';

// export const BIG_ZERO = new BigNumber(0);
// export const BIG_ONE = new BigNumber(1);
// export const BIG_NINE = new BigNumber(9);
// export const BIG_TEN = new BigNumber(10);

export const handleStake = async (amount, decimals) => {
	// const { account, library } = useActiveWeb3React();
	// const contract = getContract(account, stakeZWZAbi, library);
	// return contract.methods
	// 	.deposit(new BigNumber(amount).times(BIG_TEN.pow(decimals)).toFixed())
	// 	.send({ from: account })
	// 	.on('transactionHash', (tx) => {
	// 		console.log(tx);
	// 		return tx.transactionHash;
	// 	});
};
